// react-dom shim for React Native
// @clerk/clerk-react imports react-dom but only uses it for web rendering.
// In React Native, we redirect it here so the bundler doesn't fail.
module.exports = {};
